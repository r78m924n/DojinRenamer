# DojinRenamer
FANZA &amp; DLsite 自動リネーム＆整理ツール

FANZA（同人）および DLsite の作品ファイル（ZIPや動画など）を、CID（コンテンツID）をもとに自動でスクレイピングし、統一されたルールで美しくリネーム・フォルダ整理を行うPythonスクリプトです。

## ✨ 主な機能

* **複数サイト・複数フォーマット対応**
  * FANZA (`d_` から始まるCID)
  * DLsite (`RJ` および `VJ` から始まるCID)
* **高度な情報取得とリネーム**
  * 作品ページから「サークル名」「タイトル」「作品形式」「販売日」「更新日」「バージョン情報」を自動取得。
  * `[サークル名][CID][作品形式][販売日] タイトル Ver1.0.zip` の形式で自動リネーム。
  * タイトル内のバージョン表記揺れや、更新日からの自動バージョン推測（`Update_YYYYMMDD`）に対応。
* **サムネイル画像の自動取得**
  * リネーム後のファイル名と同名の `.jpg` でパッケージ画像を自動保存。
  * DLsiteの作品情報を取得でき、画像URLだけ取得できない場合は、CIDから画像URLを計算して取得を試みます。
  * タイトルまたはサークル名を取得できない場合（アクセス制限・販売終了などを含む）は、リネーム・フォルダ作成・画像保存をスキップし、既存の名前を保持します。
  * 取得失敗は `targets/failed.csv` に作品IDと理由を記録し、保存後に `url.txt` から除きます。同じIDの記録は更新され、重複しません。エラーリストの保存に失敗した場合は `url.txt` を保持して終了します。
  * エラーリストにあるIDは、既存ファイル・フォルダからも自動で再処理しません。再試行するIDだけ `url.txt` に1行ずつ戻してください。情報取得に成功するとエラーリストからも除かれます。
* **スマートな実行とクリーンアップ**
  * 対象リスト（`url.txt`）をスキャンし、必要なサイトの初期化（年齢確認の突破など）だけを無駄なく実行。
  * 処理が成功したCIDは `url.txt` から自動で削除され、次回以降の重複処理を防止。

## 📁 ディレクトリ構成

ツールを実行する前に、スクリプトと同じ階層に `targets` フォルダを作成してください。

```text
.
├── DojinRenamer.py    # 本スクリプト
└── targets/           # 処理対象を入れるフォルダ（自動作成されます）
    ├── url.txt        # (任意) CIDを改行区切りで羅列したテキストファイル
    ├── d_123456.zip   # リネーム対象のファイル（FANZA）
    └── VJ123456.mp4   # リネーム対象のファイル（DLsite動画）

## 🛠 環境構築 (Setup)

このスクリプトを実行するには、いくつかの外部ライブラリが必要です。

### 1. リポジトリのクローン（またはダウンロード）
```bash
git clone [あなたのリポジトリのURL]
cd DojinRenamer

Note:
インストールされる主要なパッケージは以下の通りです。
  * selenium: ブラウザの自動操作
  * webdriver-manager: Chromeドライバーの自動管理
  * requests: Webアクセス用

  pip install selenium webdriver-manager requests
